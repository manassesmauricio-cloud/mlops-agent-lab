# MLflow — tracking de experimentos y registry de modelos
mlflow==2.14.3

# ── SDKs de proveedores LLM (instala solo el que vayas a usar) ───────────────
# Anthropic (default)
anthropic>=0.30.0

# OpenAI
# openai>=1.30.0

# Google Gemini
# google-generativeai>=0.7.0

# Groq (Llama 3.3 — rápido y con capa gratuita)
# groq>=0.9.0

# Mistral
# mistralai>=1.0.0

# ML — modelos de entrenamiento
scikit-learn>=1.4.0
pandas>=2.1.0
numpy>=1.26.0
xgboost>=2.0.0
lightgbm>=4.3.0
catboost>=1.2.0
scipy>=1.12.0

# Monitoreo operacional
psutil>=5.9.0

# HTTP y YAML
requests>=2.31.0
pyyaml>=6.0.1

# Dashboard web
streamlit>=1.35.0
python-dotenv>=1.0.0

# Base de datos (Airflow con PostgreSQL)
psycopg2-binary>=2.9.9
